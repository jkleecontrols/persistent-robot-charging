# Prefer: pip install -e ".[dev]"  (see pyproject.toml)
numpy
matplotlib
gurobipy==12.0.1   # pip wheel ships a size-limited license; larger ILPs need a full academic license
h5py               # .jld datasets in legacy/waitingtime/
pytest
# Video export needs ffmpeg on PATH (brew install ffmpeg)
# legacy/ scripts additionally use pandas, seaborn, and optionally cplex
